// This package parses and serializes XML with processEntities:false (see xml/parse.ts and xml/build.ts): it never decodes or encodes entities itself, so text-node and attribute values in its model are stored exactly as they appear in the source XML (see typed/util.ts's own decodeEntities for the read-side counterpart). This means any new raw string a writer wants to store into a text node's value or an attribute's value MUST be encoded here first, or a literal '&', '<', etc. would corrupt the serialized XML. Mirrors odf.js's own src/xml/entities.ts encodeXmlText, needed here for the identical reason: typed/xlsx/build.ts is this package's first writer of genuinely new XML content (cell text, sheet names, defined-name formulas) that did not already exist, entity-encoded, in a source document.
export function encodeXmlText(value: string): string {
  return value.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;').replace(/'/g, '&apos;');
}
